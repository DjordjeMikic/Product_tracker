import styled from 'styled-components';
import { black, shine } from '../../globalStyles/colors';

export const Container = styled.div`
    height: 100vh;
    width: 100%;
    justify-content: flex-start !important;
    padding: 75px;
    background-color: ${black};
    color: ${shine};
`;